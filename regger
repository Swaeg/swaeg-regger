#!/usr/bin/env php
<?php

set_time_limit(0);

$app = require_once __DIR__ ."/app/bootstrap.php";

$console = $app["console"];
$console->add(new Commands\CsvCommand());
$console->add(new Commands\ResetCommand());
$console->add(new Commands\InitCommand());
$console->run();
